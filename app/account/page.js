export const metadata = {
  title: "Guest Area",
};
const Page = () => {
  return (
    <div>
      <h1>Your Account</h1>

      <button active={true} className="input">
        Fares Now
      </button>
    </div>
  );
};

export default Page;
